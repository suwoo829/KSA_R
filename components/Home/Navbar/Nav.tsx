// Create a temporary Nav.tsx file
import React from "react";

type Props = {
  openNav: () => void;
};

const Nav = ({ openNav }: Props) => {
  return <div>Navigation</div>;
};

export default Nav;
